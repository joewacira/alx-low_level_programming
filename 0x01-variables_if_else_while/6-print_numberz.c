#include <stdio.h>

/**
 * main - It indicates the main entry of a program.
 *
 * Return: 0 return value 0 if program runs successfully
 */
int main(void)
{
	int x;

	for (x = 48; x < 58; x++)
	{
		putchar(x);
	}
	putchar('\n');

	return (0);
}
